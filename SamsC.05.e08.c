/* Write a function that receives two numbers as arguments */
/* The function should divide the first number by the second. */
/* Don't divide the second number if it's zero. */

float divide( int x, y);

float divide( int x, y)
{
    if (y != 0 )
    {
        z = x / y;
        return z;
    }
}