/* write code that declares a type float variable */
/* declares and initializes a pointer to the variable */
/* and declares an initializes a pointer to the pointer */

float ptr[1][1] = { 5.0 };

// ptr[0][0] is a float variable initialized to 5.0
// ptr[0] is a pointer to &ptr[0][0]
// ptr is a pointer to the pointer ptr[0]